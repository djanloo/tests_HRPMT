filters:
  and:
    - file.hasTag("tipo/bug")
    - or:
        - status == "aperto"
        - status == "in-corso"
views:
  - type: table
    name: Bug aperti
    groupBy:
      property: project
      direction: ASC
    order:
      - file.name
      - status
      - updated
